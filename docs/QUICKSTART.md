# Quickstart

1. Clone
   - `git clone git@github.com:Hans-TFX/TFXHub.git`
2. Open in VS Code
   - `code .`
3. Restore and build
   - `dotnet restore`
   - `dotnet build`
4. Run Host
   - `dotnet run --project src/TFXHub.Host`
5. Run tests
   - `dotnet test`
