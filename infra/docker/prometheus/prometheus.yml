global:
  scrape_interval: 15s

# TODO: Install prometheus-net (NuGet) in TFXHub.Host and add:
#   builder.Services.AddPrometheusExporter();
#   app.MapPrometheusScrapingEndpoint("/metrics");
# Then uncomment the scrape_configs block below.
#
# scrape_configs:
#   - job_name: 'tfxhub-host'
#     static_configs:
#       - targets: ['host1:5000', 'host2:5000']
